package mcp

// RegisterHandlers registers all the MCP handlers
func (s *Server) RegisterHandlers() {
	// Handlers are now registered directly when creating the server
	// See server.go for tool registration
}